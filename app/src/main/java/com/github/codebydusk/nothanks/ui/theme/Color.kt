package com.github.codebydusk.nothanks.ui.theme

import androidx.compose.ui.graphics.Color

// ── Golden Silence palette (settings page duo-tone) ──
val GoldenYellow = Color(0xFFFFCB47)
val DarkCharcoal = Color(0xFF1E1E24)

// Derived shades for surfaces / containers
val GoldenYellowLight = Color(0xFFFFD86A)   // lighter gold for surfaces in light mode
val DarkCharcoalLight = Color(0xFF2A2A35)   // slightly lighter charcoal for surfaces in dark mode
val GoldenMuted = Color(0xFFC4A53A)         // muted gold for secondary text (dark mode)
val CharcoalMuted = Color(0xFF3D3A20)       // muted charcoal for secondary text (light mode)